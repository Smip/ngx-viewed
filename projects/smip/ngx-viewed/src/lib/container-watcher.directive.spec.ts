import {ContainerWatcherDirective} from './container-watcher.directive';

describe('ContainerWatcherDirective', () => {
  it('should create an instance', () => {
    const directive = new ContainerWatcherDirective();
    expect(directive).toBeTruthy();
  });
});
