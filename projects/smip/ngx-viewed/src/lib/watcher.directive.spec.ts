import {WatcherDirective} from './watcher.directive';

describe('WatcherDirective', () => {
  it('should create an instance', () => {
    const directive = new WatcherDirective();
    expect(directive).toBeTruthy();
  });
});
